name: flutter_project
description: Example Flutter app — playground for code generated by packages/generator.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.0.0
  dartz: ^0.10.1
  shimmer: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
