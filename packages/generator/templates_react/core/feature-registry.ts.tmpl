/// Feature registry for React Router.
///
/// Fix vs v1: `FeatureRegistry.register()` side-effect pattern replaced with
/// a static `allDescriptors` array. Mutable registration at module-import time
/// is order-dependent and fragile with bundler tree-shaking.
///
/// The generator populates the GENERATED:imports and GENERATED:entries markers.
/// GENERATED:registrations is kept for backward compatibility with the RegistryWriter
/// but now populates the descriptors array, not a mutation call.
import type { RouteObject } from 'react-router-dom';

// GENERATED:imports BEGIN
// GENERATED:imports END

export interface FeatureDescriptor {
  readonly id: string;
  readonly title: string;
  readonly path: string;
  readonly icon?: string;
}

/// Static descriptor list — deterministic, tree-shake safe.
export const allDescriptors: FeatureDescriptor[] = [
  // GENERATED:registrations BEGIN
  // GENERATED:registrations END
];

export const routes: RouteObject[] = [
  // GENERATED:entries BEGIN
  // GENERATED:entries END
];
