name: flutter_generator_mcp
description: MCP server wrapping the Flutter feature generator for AI IDEs.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  yaml: ^3.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.15

executables:
  flutter-generator-mcp: main
