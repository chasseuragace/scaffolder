name: scaffolder_mcp
description: MCP server wrapping the scaffolder (framework-agnostic feature generator) for AI IDEs.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  yaml: ^3.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.15

executables:
  scaffolder-mcp: main
