# Simple preset — minimal CRUD without bells and whistles.
features:
  pagination: false
  fake_repository: true
  search: false
  forms: true
  optimistic_updates: true
  mock_data_generation: true
  json_serialization: true
  shimmer_loading: false
  unit_tests: false
