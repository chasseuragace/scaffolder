# Enterprise preset — same surface as `standard` for v1; reserved for
# future flags (offline cache, retry policies, telemetry hooks).
features:
  pagination: true
  fake_repository: true
  search: true
  forms: true
  optimistic_updates: true
  mock_data_generation: true
  json_serialization: true
  shimmer_loading: true
  success_feedback: true
  unit_tests: true
