name: flutter_generator_mcp
description: MCP server for Flutter feature generator
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # No external dependencies - pure Dart

dev_dependencies:
  lints: ^3.0.0
  test: ^1.26.3

executables:
  flutter-generator-mcp: main
